#include <stdio.h>
void Hello_world(void);
void Values(char, char);

int main (void)
{
  char a, b;
  int sum = 0;
  Hello_world();
  Values(a,b);
  printf("\nSum is %d\n", sum);
  return 0;
}
